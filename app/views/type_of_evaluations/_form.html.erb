<%= form_with(model: type_of_evaluation, local: true) do |form| %>
  <% if type_of_evaluation.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(type_of_evaluation.errors.count, "error") %> prohibited this type_of_evaluation from being saved:</h2>

      <ul>
      <% type_of_evaluation.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :type_of_evaluation_name %>
  </div>

  <div class="field">
    <%= form.label :weight %>
    <%= form.number_field :weight, id: :type_of_evaluation_weight %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
