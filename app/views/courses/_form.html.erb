<%= form_with(model: course, local: true) do |form| %>
  <% if course.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(course.errors.count, "error") %> prohibited this course from being saved:</h2>

      <ul>
      <% course.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :level %>
    <%= form.text_field :level, id: :course_level %>
  </div>

  <div class="field">
    <%= form.label :section %>
    <%= form.text_field :section, id: :course_section %>
  </div>

  <div class="field">
    <%= form.label :graduation_year %>
    <%= form.number_field :graduation_year, id: :course_graduation_year %>
  </div>

  <div class="field">
    <%= form.label :Teacher_id %>
    <%= form.text_field :Teacher_id, id: :course_Teacher_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
