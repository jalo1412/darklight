<%= form_with(model: note, local: true) do |form| %>
  <% if note.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(note.errors.count, "error") %> prohibited this note from being saved:</h2>

      <ul>
      <% note.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :date %>
    <%= form.date_select :date, id: :note_date %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_field :description, id: :note_description %>
  </div>

  <div class="field">
    <%= form.label :Student_id %>
    <%= form.text_field :Student_id, id: :note_Student_id %>
  </div>

  <div class="field">
    <%= form.label :Lecture_course_id %>
    <%= form.text_field :Lecture_course_id, id: :note_Lecture_course_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
